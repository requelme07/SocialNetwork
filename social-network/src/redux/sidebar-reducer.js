
let initialState = {
    friends: [
        { id: 1, name: "Fred" },
        { id: 2, name: "Bred" },

        { id: 3, name: "Martin" },
    ],
}

const sidebarReducer = (state = initialState, action) => {


    return state;
}

export default sidebarReducer;